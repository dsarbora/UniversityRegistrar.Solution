@using UniversityRegistrar.Models;

<h1>Student Index</h1>

@if(Model.Count!=0)
{
    <ul>
    @foreach (Student student in @Model)
    {
        <li>@student.GetName(), @student.GetDateOfEnrollment()</li>
    }
    </ul>
}

<a href="/students/new">Add a student</a>
<a href="/">Back home</a>